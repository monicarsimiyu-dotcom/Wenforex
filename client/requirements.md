## Packages
recharts | For real-time trading charts
framer-motion | Smooth animations for UI elements and page transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
WebSocket connection required at /ws for real-time price updates.
Paystack integration flows through the backend; frontend handles the redirect URL.
Theme settings: Dark mode default, Primary #FFA127, Background #373737.
